//! Contract tests for HelmVault, run against Odra's in-memory test backend.
//! Run with: `cargo odra test`
//!
//! Scaffold placeholder — the contract bodies are `todo!()`. Implement them,
//! then unignore and flesh out these tests (deploy, pause/resume, access
//! control, record_rebalance emitting RebalanceRecorded).

#[test]
#[ignore = "scaffold: implement HelmVault, then enable"]
fn vault_records_rebalance() {
    // TODO: deploy HelmVault, call record_rebalance, assert the event + count.
}
